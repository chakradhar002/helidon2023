spring.application.name=myspringdemo
server.port = 9090

# MySQL Configuration for Railway Cloud
spring.datasource.url=jdbc:mysql://${RAILWAY_DB_HOST}:${RAILWAY_DB_PORT}/${DATABASE_NAME}?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=${RAILWAY_DB_USERNAME}
spring.datasource.password=${RAILWAY_DB_PASSWORD}
# Hibernate Configuration (optional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional: Connection Pool Configuration (HikariCP)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
